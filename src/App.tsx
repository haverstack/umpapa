import { h } from 'preact';

export default () => {
  return (
    <div class="bg-yellow-100 p-4 h-screen">
      <div class="bg-white container rounded-sm mx-auto p-4">
        Hello, world!
      </div>
    </div>
  );
};
